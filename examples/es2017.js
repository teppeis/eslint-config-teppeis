'use strict';

/* eslint-disable no-magic-numbers, no-unused-vars */

// async/await
const foo = async a => fetch(a);

// comma-dangle for "functions"
function comma(
  a,
  b,
) {}

comma(
  1,
  2,
);
